function rokuconvert() {
  if [ -z "$1" ] || [ -z "$2" ]; then
    echo "Usage: rokuconvert <input_file> <output_basename>"
    return 1
  fi
  
  ffmpeg -i "$1" -c:v libx264 -preset medium -crf 23 -c:a aac -b:a 128k -movflags +faststart "$2".mp4
}
